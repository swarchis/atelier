# Secrets — never commit any env file (except example templates)
.env
.env.*
!.env.example
!.env.*.example

# Dependencies / build output
node_modules/
dist/

# OS / editor cruft
.DS_Store

# Local agent test harness — tooling for driving the app in a browser, not part
# of the product. Kept out of the repo deliberately.
.claude/skills/
api/scripts/test-session.js
api/scripts/test-brand.js
